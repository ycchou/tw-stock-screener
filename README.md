# 📈 台股均線糾結篩選器 (TW Stock Screener PWA)

這是一個專為台股投資人設計的 **技術分析篩選工具**。
透過強大的篩選演算法，找出目前處於「均線糾結」狀態的股票，這通常意味著股價正在積蓄動能，可能即將出現大行情的突破點。

本專案採用 **PWA (Progressive Web App)** 技術，並使用 **FastAPI** 整合前後端，提供極致流暢的使用體驗。

![各裝置支援](https://img.shields.io/badge/Device-Desktop%20%7C%20Mobile-blue)
![架構](https://img.shields.io/badge/Architecture-Integrated%20(Port%208000)-green)
![部署](https://img.shields.io/badge/Deploy-Docker%20%7C%20GCP-orange)

---

## ✨ 核心功能

*   **均線糾結篩選**：自訂關注的均線 (5MA, 10MA, 20MA, 60MA...)。
*   **參數高度客製**：可調整「糾結幅度 (%)」與「持續天數」，找出最符合您策略的標的。
*   **即時 K 線圖**：整合 TradingView等級的互動式圖表，點擊股票立即查看走勢。
*   **PWA 支援**：支援「安裝」到桌面或手機主畫面，無須透過 App Store，體驗如同原生 App。
*   **跨平台支援**：響應式設計 (RWD)，在電腦、平板、手機上皆完美呈現。

---

## 🚀 快速啟動 (Quick Start)

本專案採用 **單一端口整合架構**，您**不需要**分別啟動前後端。只需啟動後端服務，即可同時使用。

### 💻 Windows 用戶 (推薦)

我們準備了一鍵啟動腳本，自動幫您設定環境與執行。

1.  在專案資料夾中找到 **`start_server.ps1`** 檔案。
2.  右鍵點擊，選擇 **「使用 PowerShell 執行 (Run with PowerShell)」**。
3.  等待黑色視窗出現，服務即啟動完成！

### 🐧 Mac / Linux / 進階用戶

如果您習慣使用終端機 (Terminal)：

```bash
# 1. 進入後端目錄
cd backend

# 2. 建立虛擬環境 (首次執行需做)
python -m venv venv

# 3. 啟動虛擬環境
# Windows:
.\venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 4. 安裝依賴套件
pip install -r requirements.txt

# 5. 啟動服務 (同時包含網頁與 API)
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

---

## 🌐 如何使用 (Access)

服務啟動後，請打開瀏覽器訪問：

### 電腦端
👉 **[http://localhost:8000](http://localhost:8000)**

> ⚠️ **重要提示**：請勿訪問 Port 3000，該端口已不再使用。

### 手機端 (區網連線)
啟動腳本 (start_server.ps1) 會在視窗中顯示您的 **區網 IP (Mobile URL)**，例如：
`http://192.168.1.105:8000`

確保手機連上同一個 Wi-Fi，輸入該網址即可使用。

---

## ☁️ 雲端部署 (Deployment)

本專案已完全容器化，支援 Docker 與 Google Cloud Platform (GCP) 部署。

### Docker 部署

```bash
# 1. 構建映像檔
docker build -t stock-screener .

# 2. 執行容器 (對應到本機 8080 Port)
docker run -p 8080:8080 stock-screener
```
訪問：`http://localhost:8080`

### Google Cloud Run (Serverless)

適合完全不想管伺服器的用戶，自動擴展且支援 HTTPS。

1.  安裝 **Google Cloud CLI** 並登入 (`gcloud auth login`)。
2.  執行部署指令：

```bash
gcloud run deploy tw-stock-screener \
  --source . \
  --region asia-east1 \
  --allow-unauthenticated
```
部署完成後，GCP 會產生一個永久的 HTTPS 網址。

---

## 📂 專案結構

```plaintext
tw-stock-screener/
├── backend/                # 後端核心
│   ├── main.py             # 程式入口 (API + 靜態檔案掛載)
│   ├── requirements.txt    # Python 依賴清單
│   ├── services/           # 業務邏輯 (篩選運算、資料抓取)
│   └── tests/              # 自動化測試 (API與整合測試)
│
├── frontend/               # 前端介面
│   ├── index.html          # 單頁應用入口
│   ├── js/                 # 邏輯控制 (API串接、圖表繪製)
│   ├── css/                # 樣式表
│   └── icons/              # PWA 圖示資源
│
├── doc/                    # 專案文件與測試計畫
├── Dockerfile              # Docker 構建設定
├── start_server.ps1        # Windows 快速啟動腳本
└── README.md               # 專案說明文件
```

---

## 🛠️ 開發者資訊 (Developers)

- **前端技術**：HTML5, CSS3, Vanilla JS (無框架), Lightweight Charts
- **後端技術**：Python 3.11+, FastAPI, Pandas, yfinance
- **API 文件**：啟動後訪問 `/docs` 可查看 Swagger UI

---

_Created by Antigravity_
